@model IEnumerable<ATTUT.Data.Models.CategoryModel>
@{
    ViewData["Title"] = "Category List";
    int i = 1;
}

<div class="form-w3layouts">
    <div class="row">
        <div class="col-lg-12">
            <section class="panel">
                <header class="panel-heading">
                    Category List
                    <span class="tools pull-right">
                        <a class="btn btn-sm btn-danger" href="/Master/AddCategory">Add</a>
                    </span>
                </header>
                <div class="panel-body">
                    <table class="table table-bordered table-responsive-md" id="tblCategory">
                        <thead>
                            <tr>
                                <th>Sr. No.</th>
                                <th>Category Name</th> 
                                <th>Is Active</th>
                                <th>#</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null)
                            {
                                if (Model.Count() > 0)
                                {
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@(i++)</td>
                                            <td>@Html.DisplayFor(modelItem => item.CategoryName)</td>
                                            <td>@Html.DisplayFor(modelItem => item.IsActive)</td>
                                            <td>
                                                <a href="/Master/EditCategory?Id=@item.CategoryID"><i class="fa fa-edit"></i></a>
                                            </td>
                                        </tr>
                                    }
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </section>
        </div>
    </div>
</div>

@section scripts{
    <script type="text/javascript">
        $(function () {
            $("#tblCategory").dataTable({ "ordering": true, 'iDisplayLength': 100 });
        });
    </script>
}
@model IEnumerable<ATTUT.Data.Models.Basics.StateModel>
@{
    ViewData["Title"] = "State List";
    int i = 1;
}

<div class="form-w3layouts">
    <div class="row">
        <div class="col-lg-12">
            <section class="panel">
                <header class="panel-heading">
                    state List
                    <span class="tools pull-right">
                        <a class="btn btn-sm btn-danger" href="/BasicsMaster/AddState">Add</a>
                    </span>
                </header>
                <div class="panel-body">
                    <table class="table table-bordered table-responsive-md" id="tblState">
                        <thead>
                            <tr>
                                <th>Sr. No.</th>
                                <th>Country Name</th>
                                <th>State Name</th>
                                <th>State Remarks</th>
                                <th>Is Active</th>
                                <th>#</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null)
                            {
                                if (Model.Count() > 0)
                                {
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@(i++)</td>
                                            <td>@Html.DisplayFor(modelItem => item.CountryName)</td>
                                            <td>@Html.DisplayFor(modelItem => item.StateName)</td>
                                            <td>@Html.DisplayFor(modelItem => item.StateRemarks)</td>
                                            <td>@Html.DisplayFor(modelItem => item.IsActive)</td>
                                            <td>
                                                <a href="/BasicsMaster/EditState?Id=@item.StateId"><i class="fa fa-edit"></i></a>
                                            </td>
                                        </tr>
                                    }
                                }
                            }
                        </tbody>
                    </table>
                </div>
            </section>
        </div>
    </div>
</div>

@section scripts {
    <script type="text/javascript">
        $(function () {
            $("#tblState").dataTable({ "ordering": true, 'iDisplayLength': 100 });
        });
    </script>
}